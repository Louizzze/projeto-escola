function hideScreenLock(){ 
    let screenlock = document.getElementById("screenlock");
    screenlock.className = "hide";
}

function showScreenLock(){
    let screenlock = document.getElementById("screenlock");
    screenlock.className = "show";
}